import { BrowserRouter, Routes } from "react-router-dom";

function Router() {
  return (
    <BrowserRouter>
      <Routes>{/*Route를 추가해주세요! */}</Routes>
    </BrowserRouter>
  );
}

export default Router;
